# TODO / agent handoff

## Status (2026-09-26)

**Tip:** `biltoo-2709.7-glyphs-bg-kind-annotate` (base `b65f69e`).

### 2709.7 — Glyph backgrounds + host Kind annotate
- “Show text in boxes” draws paper fill + kind-coloured edge under glyphs
- `TextRegionKindAnnotate` labels Header/Footer/PageNumber on load (native + OCR)

Still not full layout analysis (no columns / true headings).

### Apply
```bash
git pull --ff-only …/biltoo-2709.7-glyphs-bg-kind-annotate-b65f69e.bundle HEAD
```
